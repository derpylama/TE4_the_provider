should we have api always verify every action or only logging 
every action {
    slow
gaurantes that the organisation always has access = bans from provider are instant
}

verify  only logging in {
    faster api no need to verify everytime only when our token expired or session_key (30min)
    bans would have delay until token is expired
}
